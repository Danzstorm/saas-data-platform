# Dev environment — local execution.
project:
  env: dev

paths:
  bronze: "data/bronze"
  silver: "data/silver"
  gold: "data/gold"
  quarantine_root: "data"
  quality_logs: "data/shared/quality_logs"

spark:
  shuffle_partitions: 8
  log_level: "WARN"
